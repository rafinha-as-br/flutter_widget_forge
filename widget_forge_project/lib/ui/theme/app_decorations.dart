import 'package:flutter/material.dart';

class AppDecorations {
  AppDecorations._();

  // Border Radius Constants
  static final BorderRadius borderRadiusDefault = BorderRadius.circular(12);
  static final BorderRadius borderRadiusLarge = BorderRadius.circular(15);
  static final BorderRadius borderRadiusSmall = BorderRadius.circular(8);

  // Padding Constants
  static const EdgeInsets paddingDefault = EdgeInsets.all(8.0);
  static const EdgeInsets paddingCard = EdgeInsets.symmetric(horizontal: 15, vertical: 17);
  static const EdgeInsets paddingScreenDefault = EdgeInsets.symmetric(horizontal: 20, vertical: 8);
  
  // Spacing Constants
  static const double spacingDefault = 8.0;
  static const double spacingMedium = 12.0;
  static const double spacingLarge = 20.0;
}
